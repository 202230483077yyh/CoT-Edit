# Code Edit Prediction Evaluation - Requirements
# ==============================================

# Core dependencies
requests>=2.28.0
tqdm>=4.64.0

# For API calls to Claude/Anthropic
anthropic>=0.18.0

# For tokenizer support (optional, only needed for --is_chat mode)
transformers>=4.30.0

# ==============================================
# Optional Dependencies
# ==============================================

# For local model deployment (test_local_model.sh)
# vllm>=0.4.0

# Note: torch is required by transformers/vllm but not listed here
# as it depends on your system (CPU/GPU). Install separately:
#
# CPU only:
#   pip install torch
#
# CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# For vLLM (GPU required):
#   pip install vllm
